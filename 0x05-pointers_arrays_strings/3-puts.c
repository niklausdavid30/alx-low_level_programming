#include "main.h"
#include <string.h>

/**
 * _puts - prints a string, followed by a new line,
 * @str: pointer to the string to print
 * Return: void
 */

void _puts(char *str)
{
	puts(str);
	_putchar('\n');
}
